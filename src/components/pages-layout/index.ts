import { PagesRoot, PageHeader, PageIcon, PageTitle, PageActions, PageContent } from './pages-root';

export const Pages = {
  Root: PagesRoot,
  Header: PageHeader,
  Icon: PageIcon,
  Title: PageTitle,
  Actions: PageActions,
  Content: PageContent,
};

export const Page = PagesRoot;

export { PagesRoot, PageHeader, PageIcon, PageTitle, PageActions, PageContent };
